{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "메인 창 기본 권한. 파일시스템·셸·HTTP 플러그인은 의도적으로 열지 않는다 — 모든 디스크 접근은 Rust 커맨드를 거친다.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
